package com.gumlapolytechnic.gpconnect

import android.app.Application

/**
 * Application entry point. Hosts the dependency container (AppContainer)
 * starting from the mock-authentication phase.
 */
class GPConnectApplication : Application()
